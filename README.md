# autostart-scripts
There are some autostart scripts using in the boot process of dwm (a simple dynamic windows manager) which content the status bar displayment, wallpaper setting, vol and blacklight control.
